[project]
name = "asl-shop"
version = "0.1.0"
description = "Sign language image editor"
requires-python = ">=3.10"
dependencies = [
    "mediapipe<1.0",
    "numpy",
    "opencv-contrib-python",
    "pandas",
]

[tool.uv]
package = false
